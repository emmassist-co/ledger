models:
  # Reliable validation profile for real runs over OpenRouter.
  # Slightly more expensive than the DeepSeek local defaults, but materially less likely
  # to stall on long multi-call document passes while we harden the pipeline.
  section_note: openai/gpt-4.1-mini
  claims: openai/gpt-4.1-mini
  index: openai/gpt-4.1-mini
  level_1: openai/gpt-4.1-mini
  level_2: openai/gpt-4.1-mini
  level_3: openai/gpt-4.1-mini

pipeline:
  output_root: documents
  max_section_chars: 12000
  enable_resolver: false
